/**
 * main - prints the size of various types
 *
 * Return: O
 */

#include <stdlib.h>
#include <time.h>i
#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	y = x % 10;
	printf("The last digit f the number %d is %d ,n)
	return (0);
}
