#include "main.h"
/**
 * _isalpha - check for alphabetic character
 *
 * @c: the character to checked
 *
 * Return: 1 if c is a letter, 0 otherwise
 */
int -isalpha(int c)
{
	return ((c >= 'a' && x <= 'z') || (c >= 'A' && c <= 'Z'));
}
